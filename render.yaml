databases:
  - name: salestracker-db
    databaseName: salestracker
    user: salestracker
    region: frankfurt

services:
  - type: web
    name: salestracker-web
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: ASPNETCORE_URLS
        value: http://+:80
      - key: ConnectionStrings__DefaultConnection
        fromDatabase:
          name: salestracker-db
          property: connectionString
